<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WalletTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/dark_midnight_blue</item>
        <item name="android:windowBackground">@color/light_ashen</item>

        <item name="android:textViewStyle">@style/AppTextView</item>
        <item name="editTextStyle">@style/AppEditText</item>
        <item name="buttonStyle">@style/AppButton</item>

    </style>

    <style name="Theme.WalletTracker" parent="Base.Theme.WalletTracker"/>
</resources>